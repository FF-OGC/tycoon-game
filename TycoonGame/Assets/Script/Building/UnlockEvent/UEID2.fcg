import "StdLibrary.fcc" as StdLib
import "EditorGenLib.fcc" as EditorLib

graph UEID2 {
    //Executed when entity is created

    event UNLOCK_EVENT() {
        var entity = EResSceneNewScene.Wall1BrokenSet
        LogInfo("entity " + entity)
        SetActive(entity<Entity>,false)
    }
}