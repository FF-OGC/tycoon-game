import "StdLibrary.fcc" as StdLib
import "EditorGenLib.fcc" as EditorLib

graph Script {
    //Executed when entity is created
    event OnAwake() {

        while true{
            var _parent = GetParent(GetParent(thisEntity))
            thisEntity<Text>.Content = _parent<Building>.Value + "PER HIT"
            WaitForMillisecond(2000)
        }
    }

}